<?xml version="1.0" encoding="utf-8"?>

<LinearLayout android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    xmlns:android="http://schemas.android.com/apk/res/android">

        <EditText
            android:id="@+id/editTextTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal"
            android:hint="@string/fragment_note_title"
            android:inputType="text"
            android:textSize="@dimen/fragment_note_text_view_text_size"
            android:orientation="vertical"/>

        <EditText
            android:id="@+id/editTextText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/fragment_note_create_margin_top"
            android:gravity="start"
            android:hint="@string/fragment_note_text"
            android:inputType="textMultiLine"
            android:orientation="vertical"/>

        <Button
            android:id="@+id/saveButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/fragment_note_create_save"
            app:layout_constraintTop_toBottomOf="@id/editTextText"
            android:orientation="vertical"/>
</LinearLayout>
